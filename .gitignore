# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.venv312

# Alembic migration files (should be version controlled)
# Alembic configuration (should be version controlled)
# Database files (should NOT be version controlled)
*.db
*.sqlite
*.sqlite3

# Alembic version directory contents (should be version controlled)
# Alembic specific generated files (optional to exclude)
alembic/versions/README
